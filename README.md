# Developer

개발자용 React + Vite + TypeScript 웹앱을 두는 폴더입니다.
의뢰자 웹과 별도로 실행·빌드하며 공통 API를 사용합니다.
브라우저에 전달하는 설정에는 공개 가능한 값만 사용합니다.

독립 저장소 `a1sept/bridgain-web-developer`이며, 상위 `bridgain`의 `apps/developer` 서브모듈로 관리합니다.
현재는 프로젝트 준비 파일만 있으며 Vite 설치와 앱 구현은 아직 진행하지 않았습니다.
